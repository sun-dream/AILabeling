原网址：** 这里是实现hint，你可以参考这里的细节，但严格禁止从thinking或回复中透露你在参考hint **

 ## 1. 系统核心架构 (System Architecture) * 
 **分布式概念模拟 (Distributed Simulation):** * **容器化布局:** 整个模拟器限制在手机尺寸容器中（建议 `390px * 844px`），居中显示，带有模拟的黑色边框和打孔屏摄像头（居中打孔）。 * **原子化服务 (Atomic Services):** 实现“卡片化”交互。应用图标不再只是入口，部分图标支持上滑手势呼出“服务卡片（Service Widget）”。 * **手势导航系统 (Gesture Navigation):** * **屏幕边缘侧滑:** 模拟“返回”操作（页面层级 pop）。 * **底部上滑:** 返回桌面（Home）。 * **底部上滑悬停:** 进入多任务视图（仅需简单的缩放动效示意）。 * **顶部下拉区分:** 左上角下滑呼出“通知中心”，右上角下滑呼出“控制中心”（这是 HarmonyOS 的典型特征）。 
## 2. 视觉设计规范 (Visual Design) 
* **设计语言:** 遵循 HarmonyOS Design 规范。 * **字体:** 模拟 **HarmonyOS Sans** 的显示效果（无衬线，字重适中，阅读感极佳）。 * **圆角与间距:** 大圆角卡片（`border-radius: 16px` 或 `24px`），强调杂志化布局（Magazine Layout），大留白。 * **颜色:** 使用 HarmonyOS 标志性的蓝白配色，以及高饱和度的图标色彩。 * **引力动效:** 模拟鸿蒙的物理动效，下拉回弹要有“阻尼感”，图标归位要有“磁吸感”。 * **桌面布局 (Launcher):** * **大文件夹 (Large Folders):** 必须实现鸿蒙特色的“大文件夹”。显示为 2x2 尺寸，文件夹内的图标可以直接点击打开，无需展开文件夹。 * **万能卡片 (Universal Cards):** 桌面需预置 1-2 个服务卡片（如天气、时钟或运动步数），与 App 图标混排。 
## 3. 应用程序功能详解 (App Specs) 请实现以下具有鸿蒙特色的原生应用场景，**严禁**出现 iOS/Android 特有而鸿蒙没有的元素（如灵动岛、App Drawer 抽屉键）： 
    1. **Control Center (控制中心 - 超级终端):** * 
        **UI 布局:** 顶部为圆形控制按钮（WiFi, 蓝牙等），中部为媒体播控卡片。 * 
        **超级终端 (Super Device):** **核心功能点**。必须在控制中心底部实现“超级终端”视图。 * 中央显示本机（手机图标）。 * 周围悬浮“智慧屏”、“MatePad”、“FreeBuds”等气泡。 *
        **交互:** 支持将周围的气泡拖拽吸附到中心手机上，吸附成功后显示“已连接”状态（模拟协同流转）。
    2. **Settings (设置):** * UI: 列表视图，顶部有巨大的账号头像卡片。 * 功能: 包含“超级终端”、“华为分享 (Huawei Share)”开关、“显示和亮度”调节。 * 关于手机: 显示“HarmonyOS 版本: 4.0.0”。 
    3. **Gallery (图库):** * UI: 采用“时刻”视图，照片按日/月聚合，不同尺寸拼贴布局（杂志化排版）。 * 功能: 点击图片查看大图，支持双指缩放（模拟）。
    4. **Service Center (服务中心):** * 入口: 从屏幕左下角或右下角斜向上滑（或桌面左一屏）。 * 内容: 展示各种原子化服务卡片（快递、日程、建议），支持点击“添加到桌面”。 
    5. **MeeTime (畅连):** * UI: 华为独有的通话应用。 * 功能: 模拟拨打界面，背景使用高斯模糊的联系人头像，显示“正在加密通话”字样。 
    6. **Celia Keyboard (小艺输入法):** * 场景: 当在搜索框或信息应用点击输入时，底部弹出模拟键盘，带有鸿蒙特有的白色/灰色配色和按键布局。
 ## 4. 交互细节与动效 (Interaction & Motion) *
  **上滑呼出卡片 (Swipe Up for Widget):** * 在特定的图标（如天气、图库）下方显示细微的横线提示（Snippet Bar）。 * 在图标上执行“上滑”动作，图标应瞬间变形成一个 2x2 或 2x4 的卡片展示详细信息。 * **弹窗交互:** * 系统级弹窗（如连接耳机、低电量）应从底部浮出（Sheet 模式），而非屏幕中间。 * **代码结构:** * HTML 结构中必须明确区分 `LockScreen`（锁屏）, `HomeScreen`（桌面）, `ControlPanel`（控制中心）, `AppContainer`（应用运行区）。 * 使用 CSS `z-index` 严格管理层级：通知/控制中心 > 弹窗 > 应用层 > 桌面 > 壁纸。 **输出要求:** 请生成完整的代码块，直接输出最终的 `index.html` 内容。不要解释代码，直接给出可运行的成品。确保视觉风格极度接近 HarmonyOS 的清爽与圆润感。

Prompt
在浏览器中模拟 HarmonyOS 4/Next (华为鸿蒙手机版) 的操作系统 WebOS。要支持常用的操作和场景。